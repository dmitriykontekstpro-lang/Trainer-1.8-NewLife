# 🦝 Trainer 1.8: Hard Racoon (NewLife Edition)

> **Интеллектуальная Система Адаптивных Тренировок**  
> *Версия 1.8.0*

Hard Racoon — это фитнес-компаньон нового поколения на базе React Native и Expo. Главное отличие — строго алгоритмический подход к прогрессии нагрузок, обеспечивающий линейный рост силы и мышечной массы через механику "Ступенчатой Нагрузки" (Step Loading).

---

## 🏗 Архитектура и Структура Модулей

Приложение следует принципам **Domain-Driven Design**, разделяя UI, Бизнес-логику и Хранение данных.

### 1. 📱 UI Layer (Представление)
Визуальные компоненты, отвечающие за взаимодействие с пользователем.

*   **`App.tsx` (Корневой Оркестратор)**
    *   Управляет глобальным состоянием (`view`, `user`, `isSyncing`).
    *   Отвечает за логику "День/Время" и навигацию.
    *   **Подмодули:**
        *   **`Lobby`**: Командный центр. Показывает план на сегодня, сводку по питанию и быстрые действия.
        *   **`Dashboard` (Движок)**: Интерфейс активной тренировки. Таймеры, подсчет повторений, метроном.
        *   **`PrepScreen`**: Режим фокусировки перед стартом (Голосовая подготовка, отсчет).
        *   **`Settings` / `Arsenal`**: Настройка шаблонов упражнений и расписания.

### 2. 🧠 Logic Core (Мозг)**
Чистые функции и утилиты, управляющие интеллектом приложения.

*   **`src/utils/progression.ts` (Движок Прогрессии)**
    *   **Роль**: Рассчитывает точный вес для каждого подхода на основе истории.
    *   **Логика**: Реализует "Step Loading" (Нагрузка → Закрепление → Повышение).
    *   **Вход**: Снимок истории (`ExerciseHistorySnapshot`) + Шаблон тренировки.
    *   **Выход**: Точный таймлайн блоков (`TimelineBlock`).

*   **`src/lib/openai.ts` (AI Тренер)**
    *   **Роль**: Генерирует персональные планы и советы по питанию.
    *   **Взаимодействие**: Анализирует профиль пользователя -> Возвращает JSON с упражнениями и КБЖУ.

*   **`src/utils/audio.ts` (Система Обратной Связи)**
    *   **Роль**: Обеспечивает звуковые и тактильные сигналы.
    *   **Функции**: Метроном (Сильные/Слабые доли), Сигналы обратного отсчета.

### 3. 💾 Data Layer (Данные)
Управление хранением, синхронизацией и целостностью данных.

*   **`src/utils/workoutDiaryStore.ts`**
    *   **Хранение**: Гибридное (AsyncStorage Offline + Supabase Cloud).
    *   **Ключевая функция**: `saveWorkoutLog` (Умное слияние данных для предотвращения потерь при повторных тренировках).
    *   **Ключевая функция**: `getExerciseSnapshot` (Быстрое извлечение "Последнего результата" для расчета прогрессии).

*   **`src/utils/foodDiaryStore.ts`**
    *   **Роль**: Трекинг калорий и макронутриентов.
    *   **Логика**: Синхронизация дневников питания с облаком и локальным кэшем.

---

## 🔄 Ключевые Потоки Данных

### 1. Конвейер "Умной Нагрузки" (Smart Load)
Как приложение решает, сколько вы будете поднимать сегодня:
1.  **Инициализация**: `App` запрашивает тренировку на "Сегодня".
2.  **Загрузка**: `progression.ts` вызывает `getExerciseSnapshot()` для получения всей истории.
3.  **Расчет**:
    *   *Новое упражнение*: Стандартный вес (15кг).
    *   *Есть история*: Проверка цикла "Step Loading".
        *   *Цикл 1-2*: Повтор веса (Закрепление).
        *   *Цикл 3*: **Повышение +2.5кг**.
4.  **Рендер**: `Lobby` отображает готовый план.

### 2. Цикл Выполнения Тренировки
1.  **Старт**: Пользователь переходит `PrepScreen` -> `Dashboard`.
2.  **Выполнение**: Пользователь делает подходы.
    *   *Фидбек*: Метроном тикает во время рабочих блоков.
    *   *Ввод*: Камера/Ручной ввод для корректировки веса.
3.  **Сохранение**: При завершении, `Dashboard` вызывает `saveWorkoutLog`.
    *   *Merge*: Новые данные объединяются с существующими за сегодня (безопасно для нескольких сессий в день).
    *   *Sync*: Данные отправляются в таблицу `workouts` в Supabase.

---

## 🛠 Технологический Стек

*   **Фреймворк**: Expo (React Native)
*   **Стилизация**: NativeWind (TailwindCSS)
*   **Бэкенд**: Supabase (PostgreSQL + RLS)
*   **AI**: OpenAI GPT-4o
*   **Состояние**: React Hooks (Local) + AsyncStorage (Persist)

---

## 🚀 Версия 1.9: Vision Code & Body Logic (Январь 2026)

### 1. 🍎 AI Food Vision 2.0 (OCR First)
Мы полностью отказались от сканирования штрих-кодов в пользу "Умного Зрения".
*   **Two-Pass Analysis**: AI сначала считывает весь текст с упаковки (БЖУ, название, вес) в режиме OCR, а затем использует эти данные для точного расчета, минимизируя галлюцинации.
*   **Result**: Точность распознавания упаковок выросла до 99%.

### 2. ⚖️ Протокол Веса (Weight Protocol)
Внедрена строгая логика отслеживания веса тела:
*   **Single Truth**: Вес хранится в Supabase в колонке `waist_user` таблицы `food_diary`.
*   **Принцип "Последний выигрывает"**: За одну дату хранится только одно значение (последнее).
*   **Синхронизация**: Вес обновляется и через Профиль, и через финальное взвешивание в конце тренировки. Во всех экранах отображается актуальный исторический вес.

---

> "Это не просто трекер, это протокол."

